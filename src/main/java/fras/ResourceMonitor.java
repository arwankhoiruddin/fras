package fras;

import java.util.LinkedList;

public class ResourceMonitor {
    private LinkedList<Double> nodeFreeDisk;
    private LinkedList<Double> nodeFreeMemory;
    private LinkedList<Double> nodeLinkToMaster;

    public void checkFreeDisk(int nodeID) {
        // to do
    }

    public void checkFreeMemory(int nodeID) {
        // to do
    }

    public void checkLinkToMaster(int nodeID) {
        // to do
    }
}
